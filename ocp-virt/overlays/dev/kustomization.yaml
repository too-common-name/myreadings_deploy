apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

patches:
- path: patches/nginx-route-patch.yaml
  target:
    kind: Route
    name: nginx-route
- path: patches/keycloak-route-patch.yaml
  target:
    kind: Route
    name: keycloak-route